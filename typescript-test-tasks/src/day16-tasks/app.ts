import { initializeUI } from "./ui";

// App initialisieren
initializeUI();

//- Verwaltung der Kartensammlung -> add, edit, delete
// Delete: Füge bei jeder Karte die Option hinzu, sie zu löschen
//Edit: Füge bei jeder Karte die Option hinzu, die Daten zu bearbeiten
//für das Ändern: sollen die Daten in das Formular geladen werden
